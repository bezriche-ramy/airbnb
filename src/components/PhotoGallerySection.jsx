import React from 'react';
import './PhotoGallerySection.css';

// PhotoGallerySection removed — keeping a stub so imports don't break
const PhotoGallerySection = () => null;

export default PhotoGallerySection;
